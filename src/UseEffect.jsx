/*

// useEffect) - React Hook that tells React DO SOME CODE WHEN (pick one):
This component re-renders
11
This component mounts
The state of a value
/ useffect(function, [dependencies])
/l 1. uselffect(() => ())
/l Runs after every re-render
// 2. useEffect(O) -> O, (1)
// Runs only on mount
/1 3. useEffect() =>
(). [value]) // Runs on mount + when value changes
/I USES
// #1 Event Listeners
// #2 DOM manipulation
I1 •3 Subscriptions (real-tine updates)
// a4 Fetching Data from an API
Il as clean up when a component unmounts

*/