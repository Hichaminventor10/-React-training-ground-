function App(){
    return < Welcome name="Hicham"/>
}


// child component 
function Welcome(){
    return <h1>Hello, {props.name}</h1>
}

using destructuring
a la place de props.name we can use {name}
function Welcome({name}){

return <h1>Hello,{name}</h1>

}

// passing multiple props 

function App(){
    return <USer name="Hicham" age={25} city="ksar kebir" />
}

function User({name,age,city}){
    return (
        <div>
        <h2>{name}</h2>
        <h2>{age}</h2>
        <h2>{city}</h2>
        </div>
    )

}
/ passing object 
function App(){
   const USer ={
    name : "emma"
    age : 27 
   }
   return <Profile User={user} />
}

function Profile({User}){
    return <h1>{USerser.name} / {USerser.age}</h1>
}